# Evidence — Phase 0 account foundation

**Date:** 2026-08-16 · **Account:** `867207177469` · **Region:** `us-east-1`

ARNs and resource ids are the non-fabricable spine of this ledger. Everything
below was read back from the AWS API after apply, not copied from the plan.

---

## Applied

`infra/terraform/account/` — `terraform apply` → **17 added, 0 changed, 0 destroyed.**

| Output | Value |
|---|---|
| `account_id` | `867207177469` |
| `region` | `us-east-1` |
| `github_deploy_role_arn` | `arn:aws:iam::867207177469:role/plant-platform-github-deploy` |
| `athena_workgroup` | `plant-platform` |
| `athena_scan_cap` | `2147483648` bytes (2 GiB per query) |
| `athena_results_bucket` | `plant-platform-athena-results-867207177469` |
| `cur_bucket` | `plant-platform-cur-867207177469` |
| `cur_report_name` | `plant-platform-cur` |

## Verified against the live API

Read back with `aws` after apply — not asserted from Terraform state.

**Cost and Usage Report — now recording.**

```
Name    plant-platform-cur
Bucket  plant-platform-cur-867207177469
Format  Parquet
Schema  RESOURCES
```

This was the time-critical item: CUR has no backfill, so billing detail only
exists from today forward. `RESOURCES` gives per-resource-id attribution, which
is the difference between "S3 cost $4" and "this bucket cost $4."

**Budgets — three, one pre-existing.**

```
My Monthly Cost Budget         10.0   MONTHLY    (AWS signup default, not ours)
plant-platform-monthly         25.0   MONTHLY    50% / 90% actual, 100% forecast
plant-platform-credit-total   100.0   ANNUALLY   50% / 80% actual
```

Ours exclude credits from the calculation. With $199.97 applied, credit-inclusive
"actual spend" reads near zero right until the balance empties — the alarms
would fire only after the money was gone.

**GitHub OIDC provider.**

```
arn:aws:iam::867207177469:oidc-provider/token.actions.githubusercontent.com
```

No long-lived AWS keys exist in either repository or in GitHub secrets. The role
is scoped by repo *and* ref (`main`, tags, `production` environment only) and is
explicitly denied `iam:UpdateAssumeRolePolicy`, so CI cannot widen its own
access.

**Athena workgroup.**

```
BytesScannedCutoffPerQuery   2147483648
EnforceWorkGroupConfiguration  True
```

Enforced, so a client cannot override it and escape the cap.

## Teardown verification

`python tools/teardown_verify.py --all-regions` → **exit 0**, 17 regions.

```
no unexpected billable resources
not enrolled (3): kafka, kinesis, redshift
```

Nothing created today is idle-billable: two empty S3 buckets, budgets, IAM, and
an Athena workgroup are all free at rest.

## Spend

$0.00 at time of writing. Everything applied today has no idle cost.

## Open

- **Free Plan ends 2026-09-09 — 23 days.** Recorded in `COSTS.md`; both projects'
  cloud phases schedule backwards from it, and the final teardown must land well
  before it.
- Confirm what happens at the plan boundary — account closure vs conversion to
  paid. If it converts and anything is still running, that becomes a real bill.
